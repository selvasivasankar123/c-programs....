#include <stdio.h>
int main()
{
    int i,start,end;
    printf("enter range:");
    scanf("%d %d",&start,&end);
    while(start<end) 
    {
        int flag=0;
    for(i=2;i<start;i++)
    {
        if(start%i==0)
        {
            flag=1;
            break;
        }
    }
        if(flag==0)
        {
         printf("\n%d",start);
        }
    start++;
    }
    return 0;
}
